apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: b2bbackups.b2b.moreiradj.fr
spec:
  group: b2b.moreiradj.fr
  names:
    categories: []
    kind: B2BBackup
    plural: b2bbackups
    shortNames: []
    singular: b2bbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for B2BBackupSpec via `CustomResource`
        properties:
          spec:
            properties:
              postgres:
                properties:
                  auth:
                    properties:
                      password:
                        type: string
                    required:
                    - password
                    type: object
                  host:
                    type: string
                  port:
                    format: uint16
                    minimum: 0.0
                    type: integer
                required:
                - auth
                - host
                - port
                type: object
              s3:
                properties:
                  auth:
                    properties:
                      access:
                        type: string
                      secret:
                        type: string
                    required:
                    - access
                    - secret
                    type: object
                  bucket:
                    type: string
                  endpoint:
                    nullable: true
                    type: string
                  region:
                    nullable: true
                    type: string
                required:
                - auth
                - bucket
                type: object
              schedule:
                type: string
            required:
            - postgres
            - s3
            - schedule
            type: object
          status:
            nullable: true
            properties:
              cronjob_last_successful_time:
                description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                format: date-time
                nullable: true
                type: string
              test_pod_last_schedule_time:
                description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                format: date-time
                nullable: true
                type: string
              test_pod_last_successful_time:
                description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                format: date-time
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: B2BBackup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
